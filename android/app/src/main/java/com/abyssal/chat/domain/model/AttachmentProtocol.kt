package com.abyssal.chat.domain.model

/** Metadata contract shared by attachment transport and presentation layers. */
object AttachmentProtocol {
    const val CIPHER_VERSION = 2
    const val KEY_BYTES = 32
}
