services:
  mirage-server:
    build:
      context: ../mirage-server
    image: mirage-server:latest
    restart: unless-stopped
    env_file:
      - path: ../mirage-server/.env
        required: false
    environment:
      ABYSSAL_BIND_ADDR: 0.0.0.0:8080
    ports:
      - "8080:8080"
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
